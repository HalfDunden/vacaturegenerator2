import React from 'react';
import ReactDOM from 'react-dom/client';
import VacatureGenerator from './components/VacatureGenerator';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VacatureGenerator />
  </React.StrictMode>
);
